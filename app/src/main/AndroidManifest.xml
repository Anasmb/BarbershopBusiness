<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.barberbusiness">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BarberBusiness"
        android:usesCleartextTraffic="true">

        <activity android:name=".AddBarbershopActivity" />
        <activity android:name=".AdminMainActivity" />

        <activity android:name=".AdminLoginActivity" />
        <activity android:name=".TimeActivity" />
        <activity android:name=".AddServiceActivity" />
        <activity android:name=".ServiceActivity" />
        <activity android:name=".AddBarberActivity" />
        <activity android:name=".BarbersActivity" />
        <activity android:name=".BarbershopMainActivity" />
        <activity android:name=".BarbershopLoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDItAfw9zVSHExpyj6YtPd0XyNQfYudxTE" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
    </application>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>